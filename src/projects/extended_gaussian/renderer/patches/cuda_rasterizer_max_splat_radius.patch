diff --git a/cuda_rasterizer/forward.cu b/cuda_rasterizer/forward.cu
index 9b3ee77..bb55075 100644
--- a/cuda_rasterizer/forward.cu
+++ b/cuda_rasterizer/forward.cu
@@ -177,7 +177,8 @@ __global__ void preprocessCUDA(int P, int D, int M,
     int2* rects,
     float3 boxmin,
     float3 boxmax,
-    bool antialiasing)
+    bool antialiasing,
+    int max_splat_radius)
 {
     auto idx = cg::this_grid().thread_rank();
     if (idx >= P)
@@ -247,6 +248,8 @@ __global__ void preprocessCUDA(int P, int D, int M,
     float lambda1 = mid + sqrt(max(0.1f, mid * mid - det));
     float lambda2 = mid - sqrt(max(0.1f, mid * mid - det));
     float my_radius = ceil(3.f * sqrt(max(lambda1, lambda2)));
+    if (max_splat_radius > 0)
+        my_radius = min(my_radius, static_cast<float>(max_splat_radius));
     float2 point_image = { ndc2Pix(p_proj.x, W), ndc2Pix(p_proj.y, H) };
     uint2 rect_min, rect_max;

@@ -256,7 +259,12 @@ __global__ void preprocessCUDA(int P, int D, int M,
     }
     else // Slightly more aggressive, might need a math cleanup
     {
-        const int2 my_rect = { (int)ceil(3.f * sqrt(cov.x)), (int)ceil(3.f * sqrt(cov.z)) };
+        int2 my_rect = { (int)ceil(3.f * sqrt(cov.x)), (int)ceil(3.f * sqrt(cov.z)) };
+        if (max_splat_radius > 0)
+        {
+            my_rect.x = min(my_rect.x, max_splat_radius);
+            my_rect.y = min(my_rect.y, max_splat_radius);
+        }
         rects[idx] = my_rect;
         getRect(point_image, my_rect, rect_min, rect_max, grid);
     }
@@ -460,7 +468,8 @@ void FORWARD::preprocess(int P, int D, int M,
     int2* rects,
     float3 boxmin,
     float3 boxmax,
-    bool antialiasing)
+    bool antialiasing,
+    int max_splat_radius)
 {
     preprocessCUDA<NUM_CHANNELS> << <(P + 255) / 256, 256 >> > (
         P, D, M,
@@ -491,6 +500,7 @@ void FORWARD::preprocess(int P, int D, int M,
         rects,
         boxmin,
         boxmax,
-        antialiasing
-        );
+        antialiasing,
+        max_splat_radius
+    );
 }
diff --git a/cuda_rasterizer/forward.h b/cuda_rasterizer/forward.h
index d16a6c9..85c22c1 100644
--- a/cuda_rasterizer/forward.h
+++ b/cuda_rasterizer/forward.h
@@ -49,7 +49,8 @@ namespace FORWARD
         int2* rects,
         float3 boxmin,
         float3 boxmax,
-        bool antialiasing);
+        bool antialiasing,
+        int max_splat_radius);

     // Main rasterization method.
     void render(
@@ -67,4 +68,4 @@ namespace FORWARD
 }


-#endif
\ No newline at end of file
+#endif
diff --git a/cuda_rasterizer/rasterizer.h b/cuda_rasterizer/rasterizer.h
index 87970ff..ee07fb3 100644
--- a/cuda_rasterizer/rasterizer.h
+++ b/cuda_rasterizer/rasterizer.h
@@ -53,7 +53,8 @@ namespace CudaRasterizer
             int* radii = nullptr,
             int* rects = nullptr,
             float* boxmin = nullptr,
-            float* boxmax = nullptr);
+            float* boxmax = nullptr,
+            int max_splat_radius = 0);

         static void backward(
             const int P, int D, int M, int R,
diff --git a/cuda_rasterizer/rasterizer_impl.cu b/cuda_rasterizer/rasterizer_impl.cu
index cf3b04c..26a2660 100644
--- a/cuda_rasterizer/rasterizer_impl.cu
+++ b/cuda_rasterizer/rasterizer_impl.cu
@@ -224,7 +224,8 @@ int CudaRasterizer::Rasterizer::forward(
     int* radii,
     int* rects,
     float* boxmin,
-    float* boxmax)
+    float* boxmax,
+    int max_splat_radius)
 {
     const float focal_y = height / (2.0f * tan_fovy);
     const float focal_x = width / (2.0f * tan_fovx);
@@ -288,7 +289,8 @@ int CudaRasterizer::Rasterizer::forward(
         (int2*)rects,
         minn,
         maxx,
-        antialiasing
+        antialiasing,
+        max_splat_radius
     );

     // Compute prefix sum over full list of touched tile counts by Gaussians
@@ -458,4 +460,4 @@ void CudaRasterizer::Rasterizer::backward(
         (glm::vec3*)dL_dscale,
         (glm::vec4*)dL_drot,
         antialiasing);
-}
\ No newline at end of file
+}
